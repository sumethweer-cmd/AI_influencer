# Business Goal: {{name}}

Read by: content-calendar (decides weekly content mix), content-director
(decides whether an item ends on a CTA beat).

```yaml
goal: reach_to_fanvue   # reach_to_fanvue | ugc_niche_mixed | {{other}}
funnel_notes: "{{e.g. 'reach+engagement content to drive traffic to Fanvue/subscriptions'}}"
content_mix: "{{e.g. '80% reach content, 20% soft CTA' or 'mostly niche/UGC, CTA rare'}}"
cta_style: "{{how a CTA beat should read for this character when one is used — never generic ad copy}}"
```

## Notes
- `reach_to_fanvue`-style characters: weekly calendar should lean toward
  delivery formats that reach new audiences (video_clip/reels), and content-director
  should treat `cta_beat` as a normal, expected feature of some items.
- `ugc_niche_mixed`-style characters: weekly calendar should keep variety across
  content_dna categories, rarely force a CTA, and prioritize niche consistency
  over funnel mechanics.
