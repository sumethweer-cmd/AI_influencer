# Constraints: {{name}}

## Content Boundaries

See `characters/CONTENT_HARD_LIMITS.md` first — those 4 rules apply
regardless of anything set below and can never be waived per-character.

```yaml
sfw_default: true
nsfw_allowed: {{true|false}}
nsfw_default_level: "{{if nsfw_allowed: suggestive/nude/explicit — the level generated by default}}"
nsfw_explicit_override: "{{if nsfw_allowed: the condition under which the most explicit tier actually generates — e.g. 'only when explicitly requested per content item' — never silently default to the most explicit level}}"
```

## Hard Avoid List (any layer, no exceptions)
- {{...}}

## Legal / Platform Compliance Notes
{{anything specific to this character's content that needs a disclaimer, age-gating, or platform-specific handling}}
