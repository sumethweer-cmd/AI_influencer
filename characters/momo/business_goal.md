# Business Goal: Momo

TODO — not covered by the behavioral DNA doc (v1.0), which is scoped to
personality/behavior only. Needs an explicit decision before Momo can be
`active` (content-calendar and content-director's cta_beat logic both depend
on this).

```yaml
goal: null   # reach_to_fanvue | ugc_niche_mixed | {{other}}
funnel_notes: null
content_mix: null
cta_style: null   # if a CTA beat is ever used, it must still pass through her
                    # "ambiguity over explicitness" and "flirting is an accent"
                    # principles from humor_style.md — never generic ad copy
explore_ratio: null   # 0.0-1.0 — % of content that tries an unproven combo vs
                        # a proven one, once content_performance_log exists (Phase 2)
```
